services:
  - type: web
    name: stick-frontend
    env: docker
    dockerfilePath: Dockerfile.frontend
    dockerContext: .
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://stick-backend.onrender.com
      - key: NODE_ENV
        value: production
    routes:
      - path: /
